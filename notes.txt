npm init -y
npm i express
git init
git add -A
git remote -v
git remote add origin https://github.com/divyanshu-dugar/playground.ai.git
git branch -m master main
git checkout master
node --watch server.js

Everything needs to by stringified in JSON
LLM Models

-- Frame Works
1. Bootstrap - Cdn, just means, you know, choose the closest data warehouse.you know. Geolocation wise to download these assets. So this just downloads faster.
Drawbacks - Limited and all them them looks kind of similar

2. Tailwind CSS - We can generate our own CSS

npm install -D tailwindcss